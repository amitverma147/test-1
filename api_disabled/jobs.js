// Moved from /api/jobs.js -> /src/server_handlers/jobs.js
// Kept here as a historical placeholder. Active handler is now in
// src/server_handlers/jobs.js and api/server.js routes to it.

module.exports = { migrated: true };
